<!DOCTYPE html>
<html>
<head>
  <title>Odot</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <!-- nav -->
  <div class="container head-container">
    <div class="nav">
      <div class="navbar-header">
        <span>
          <%= link_to "Odot", root_path %> 
        </span>the easy todo list app
        <% if user_signed_in? %>
          <ul class="nav nav-pills" style="margin-right:-50px;">
            <li><%= link_to 'Todo lists', todo_lists_path %></li>
          </ul>
        <% end %>
      </div>
        <ul class="nav nav-pills">
          <% if user_signed_in? %>
            <li class="username">Signed in as... <%= current_user.email %></li>
            <li><%= link_to "Sign out", destroy_user_session_path, :method => :delete %></li>
          <% else %>
            <li><%= link_to 'Login', user_session_path %></li>
            <li><%= link_to 'Register', new_user_registration_path %></li>
          <% end %>
        </ul>
      <br class="clear" />
    </div>
  </div>
  <% flash.each do |type, message| %>
  <div class="alert alert-warning alert-dismissable <%= type %>">
    <button type="button" class="close" data-dismiss="alert" aria-hidden="true">X</button>
    <%= message %>
  </div>
  <% end %>

<%= yield %>
<br/>
<div class="source text-center"><a href="https://github.com/DJilanov/odot" >See the source.</a></div>
</body>
</html>
